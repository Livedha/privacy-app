/* Global Styles */
body {
    font-family: Arial, sans-serif;
    background: #0f172a;
    color: white;
    margin: 0;
}

/* Header */
header {
    background: #1e293b;
    padding: 15px;
    text-align: center;
    font-size: 24px;
    font-weight: bold;
}

/* Container */
.container {
    padding: 20px;
}

/* Cards */
.card {
    background: #1e293b;
    padding: 15px;
    margin-bottom: 15px;
    border-radius: 10px;
    box-shadow: 0 0 10px rgba(0,0,0,0.3);
}

/* Buttons */
button {
    padding: 10px;
    border: none;
    background: #22c55e;
    color: white;
    border-radius: 5px;
    cursor: pointer;
}

button:hover {
    background: #16a34a;
}

/* Risk Colors */
.high {
    color: red;
    font-weight: bold;
}

.medium {
    color: orange;
    font-weight: bold;
}

.low {
    color: lightgreen;
    font-weight: bold;
}

/* Input */
input, textarea {
    padding: 10px;
    width: 100%;
    margin-top: 10px;
    border-radius: 5px;
    border: none;
}

/* Hidden Elements */
.hidden {
    display: none;
}